[package]
name = "ytkit"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib", "lib"]
name = "ytkit"

[dependencies]
common = { path = "../common" }
rstube = { path = "../rstube" }
serde_json.workspace = true
uniffi.workspace = true

[dev-dependencies]
tokio.workspace = true
